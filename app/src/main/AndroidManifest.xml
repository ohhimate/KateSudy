<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="team.mediasoft.katestudy">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KateStudy">
        <activity
            android:name=".main.presentation.CityWeatherActivity"
            android:exported="false" />
        <activity
            android:name=".main.presentation.GeneralWeatherActivity"
            android:exported="false" />
        <activity
            android:name=".main.presentation.MainActivity"
            android:exported="true"
            android:persistableMode="persistNever">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>